#include "Stack.hpp"
#include <iostream>
#include <string>

using namespace std;

class stringAlter 
{
    private:
        
    public:
        stringAlter();
        char findChar(string addStr, int finder);
        Stack* divideUp(string divStr);
};